# Contributing

Use a focused branch and include tests for behavior changes. Public interfaces require a matching update in `packages/contracts` and release notes. Never commit exports, `.env` files, OAuth credentials, webhook secrets, MCP tokens, or agent prompts/logs containing sensitive data.

Open an issue before changing authentication, authorization, persistence schema, or MCP contracts. The maintainers review security-sensitive changes and releases.
